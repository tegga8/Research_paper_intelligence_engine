# Optional heavier local ML stack. Install after the base requirements when you want
# production-grade embeddings/clustering instead of deterministic fallbacks.
sentence-transformers>=3.0
faiss-cpu>=1.8
umap-learn>=0.5
hdbscan>=0.8
bertopic>=0.16
langchain>=0.2
